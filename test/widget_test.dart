import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — Firebase requires real init for full app tests
    expect(true, isTrue);
  });
}
